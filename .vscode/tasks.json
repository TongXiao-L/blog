{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "hexo",
            "type": "shell",
            "command": "cd ${workspaceFolder} && hexo clean && hexo g && hexo s"
        }
    ]
}